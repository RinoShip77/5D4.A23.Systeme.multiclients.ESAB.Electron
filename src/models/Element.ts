export interface Element {
  elementNumber: number,
  symbol: string,
  name: string,
  group: number,
  period: number,
  price: number,
  stock: number,
  url: string,
  marketWeighting: number,
  volatility: number
}